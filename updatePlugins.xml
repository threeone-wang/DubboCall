<?xml version="1.0" encoding="UTF-8"?>
<!--
  The <plugins> element contains the description of the plugins available at this repository. Required.
-->
<plugins>
    <!--
      Each <plugin> element describes one plugin in the repository. Required.
      id - used by JetBrains IDEs to uniquely identify a plugin. Required. Must match <id> in plugin.xml
      url - path to download the plugin JAR/ZIP file. Required. Must be HTTPS
      version - version of this plugin. Required. Must match <version> in plugin.xml
    -->
    <plugin id="com.yitulin.dubbo-call" url="https://github.com/yitulin/DubboCall/raw/master/distributions/dubbo-call-1.2.2-SNAPSHOT.zip" version="1.2.2-SNAPSHOT">
        <name>DubboCall</name>
        <description><![CDATA[解析方法、参数，更便捷的通过HTTP调用。]]></description>
        <change-notes><![CDATA[
        v1.2.3-SNAPSHOT：
        bug修复。
        <br/>
        v1.2.2-SNAPSHOT：
        发起请求事件增加快捷键。
        <br/>
        v1.2.1-SNAPSHOT:
        记录文件被覆盖导致记录丢失bugfix。
        <br/>
        v1.2.0-SNAPSHOT:
        配置持久化文件独立与插件平台，导出旧版配置，支持指定配置文件。
        配置文件同级目录下，将创建记录文件。
        <br/>
        ]]></change-notes>
        <tipMessage>DubboCall插件升级啦，小伙伴赶快去体验吧！</tipMessage>
        <vendor email="yitulin@outlook.com">YituLin</vendor>



        <!--
          The <idea-version> element must match the same element in plugin.xml. Required.
        -->
        <idea-version since-build="211"/>
    </plugin>
</plugins>